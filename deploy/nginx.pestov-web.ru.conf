server {
    listen 80;
    server_name pestov-web.ru;
    location /.well-known/acme-challenge/ { root /var/www/certbot; }
    location / { return 301 https://pestov-web.ru$request_uri; }
}

server {
    listen 443 ssl;
    http2 on;
    server_name pestov-web.ru;
    ssl_certificate /etc/letsencrypt/live/pestov-web.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/pestov-web.ru/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    client_max_body_size 10m;

    location / {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_buffering off;
        proxy_read_timeout 60s;
    }
}
